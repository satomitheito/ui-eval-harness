# Compute inter-rater reliability (Cohen's kappa / Krippendorff's alpha) — placeholder
def cohen_kappa(ratings_a: list[int], ratings_b: list[int]) -> float:
    # TODO: implement properly or import from a stats lib
    return 0.0
